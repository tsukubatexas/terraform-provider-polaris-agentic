---
page_title: "polaris_rest_resource Resource"
description: |-
  Manage a Polaris REST object with generated OpenAPI operation IDs.
---

<!-- Code generated by cmd/polaris-provider-gen from apache-polaris-1.5.0. DO NOT EDIT. -->

# polaris_rest_resource

`polaris_rest_resource` maps Terraform lifecycle phases to Polaris REST operations. Use generated `*_operation_id` values when possible. Use explicit `*_method` and `*_path` only for an endpoint that is not present in the generated operation inventory.

## Example Usage

```terraform
resource "polaris_rest_resource" "catalog" {
  provider = polaris.management

  create_operation_id = "createCatalog"
  read_operation_id   = "getCatalog"
  delete_operation_id = "deleteCatalog"

  path_params = {
    catalogName = "risk"
  }

  body = jsonencode({
    catalog = {
      type = "INTERNAL"
      name = "risk"
      properties = {
        "default-base-location" = "s3://risk-warehouse/"
      }
      storageConfigInfo = {
        storageType      = "S3"
        allowedLocations = ["s3://risk-warehouse/"]
      }
    }
  })

  id_attribute = "name"
}
```

## Schema

### Optional

- `create_operation_id` (String) OpenAPI operation ID used for create.
- `read_operation_id` (String) OpenAPI operation ID used for read.
- `update_operation_id` (String) OpenAPI operation ID used for update.
- `delete_operation_id` (String) OpenAPI operation ID used for delete.
- `create_method`, `read_method`, `update_method`, `delete_method` (String) HTTP method fallback when an operation ID is not set.
- `create_path`, `read_path`, `update_path`, `delete_path` (String) HTTP path fallback when an operation ID is not set.
- `path_params` (Map of String) Values for OpenAPI path placeholders such as `{catalogName}` or `{namespace}`.
- `query_params` (Map of String) Query string parameters.
- `headers` (Map of String) Additional HTTP headers for this resource.
- `body` (String, Sensitive) JSON request body used for create and update.
- `id_attribute` (String) Dot-path in the create response used as Terraform ID, for example `name` or `principal.name`.
- `expected_status_codes` (List of Number) Accepted status codes for create/update/delete. Defaults to `200`, `201`, and `204`.

### Read-Only

- `status_code` (Number) Last HTTP status code.
- `response_body` (String, Sensitive) Last JSON response body.

## Lifecycle Notes

The generic resource cannot infer Terraform semantics from OpenAPI alone. Always choose explicit read and delete operations where Polaris supports them. Some Polaris operations, such as grant revocation, require a request body on a non-create phase; those should be handled by a typed resource once the provider grows beyond the generic bootstrap resource.
